const home = require('./home.route.js');
const product = require('./product.route.js');
const note = require('./note.route.js');
const user = require('./user.route.js');

module.exports = {
  home,
  product,
  note,
  user,
};
